"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { Check, Edit3, ExternalLink, FilePlus2, Plus, Save, Search, Trash2, X } from "lucide-react";
import type { Project } from "@/lib/projects";
import type { TopicRecord } from "@/lib/topic-data";
import { validateProjectInput, validateTopicInput } from "@/lib/content-management";

export type ContentType = "blog" | "projects" | "topics";

export function ContentTypeNavigation({ active }: { active: ContentType }) {
  return <nav className="author-content-type-nav" aria-label="Author Studio content types">
    {([['blog', 'Blog', '/author'], ['projects', 'Projects', '/author?type=projects'], ['topics', 'Topics', '/author?type=topics']] as const).map(([type, label, href]) => <Link key={type} href={href} className={active === type ? "active" : ""} aria-current={active === type ? "page" : undefined}>{label}</Link>)}
  </nav>;
}

function splitLines(value: string) { return value.split(/\n|,/).map((item) => item.trim()).filter(Boolean); }
function joinLines(value: string[]) { return value.join("\n"); }

const blankProject: Project = { slug: "", name: "", category: "", description: "", coverImage: "", status: "Prototype", stack: [], github: "", demo: "", problem: "", solution: "", architecture: [], features: [], lessons: "" };
const blankTopic: TopicRecord = { slug: "", name: "", shortName: "", description: "" };

function Message({ error, notice }: { error: string; notice: string }) { return <>{error && <p className="form-error" role="alert">{error}</p>}{notice && <p className="author-notice" role="status"><Check size={15} aria-hidden="true" /> {notice}</p>}</>; }

export function ProjectManager() {
  const [projects, setProjects] = useState<Project[]>([]); const [sha, setSha] = useState(""); const [draft, setDraft] = useState<Project | null>(null); const [search, setSearch] = useState(""); const [status, setStatus] = useState("all"); const [sort, setSort] = useState("nameAsc"); const [busy, setBusy] = useState(true); const [error, setError] = useState(""); const [notice, setNotice] = useState("");
  async function load() { setBusy(true); setError(""); try { const response = await fetch("/api/author/projects", { cache: "no-store" }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Unable to load projects."); setProjects(Array.isArray(body.projects) ? body.projects : []); setSha(body.shas?.projects || ""); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to load projects."); } finally { setBusy(false); } }
  useEffect(() => { void load(); }, []);
  const filtered = useMemo(() => projects.filter((project) => { const haystack = `${project.name} ${project.slug} ${project.category} ${project.stack.join(" ")} ${project.status}`.toLowerCase(); return (!search.trim() || haystack.includes(search.trim().toLowerCase())) && (status === "all" || project.status === status); }).sort((a, b) => sort === "nameDesc" ? b.name.localeCompare(a.name) : a.name.localeCompare(b.name)), [projects, search, sort, status]);
  function update(field: keyof Project, value: string | string[]) { setDraft((current) => current ? { ...current, [field]: value } : current); setError(""); setNotice(""); }
  async function save() { if (!draft) return; const errors = validateProjectInput(draft); if (errors.length) { setError(errors[0]); return; } setBusy(true); setError(""); try { const response = await fetch("/api/author/projects", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ projects: projects.some((item) => item.slug === draft.slug) ? projects.map((item) => item.slug === draft.slug ? draft : item) : [...projects, draft], sha }) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Unable to save project."); setSha(body.sha || sha); setProjects((current) => current.some((item) => item.slug === draft.slug) ? current.map((item) => item.slug === draft.slug ? draft : item) : [...current, draft]); setDraft(null); setNotice("Project saved to GitHub."); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to save project."); } finally { setBusy(false); } }
  function startNew() { setDraft({ ...blankProject }); setError(""); setNotice(""); }
  return <section className="author-content-manager container"><div className="author-masthead"><div className="eyebrow">Content management</div><h1>Projects</h1><p>Manage the existing project records used by the public Projects pages.</p></div><div className="author-heading"><div><div className="eyebrow">Project archive</div><h2>Build in public.</h2><p>Projects remain backed by the repository’s existing `lib/projects.ts` schema.</p></div><button className="btn btn-primary" type="button" onClick={startNew}><Plus size={16} aria-hidden="true" /> New project</button></div><Message error={error} notice={notice} /><div className="author-toolbar"><label className="author-search-field"><span className="sr-only">Search projects</span><Search size={15} aria-hidden="true" /><input aria-label="Search projects" placeholder="Search projects…" value={search} onChange={(event) => setSearch(event.target.value)} /></label><select aria-label="Filter projects by status" value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">All statuses</option>{Array.from(new Set(projects.map((project) => project.status))).map((item) => <option value={item} key={item}>{item}</option>)}</select><select aria-label="Sort projects" value={sort} onChange={(event) => setSort(event.target.value)}><option value="nameAsc">Name A–Z</option><option value="nameDesc">Name Z–A</option></select></div>{busy && !projects.length ? <p>Loading projects…</p> : <div className="author-article-list">{filtered.map((project) => <button type="button" className="author-article-row" key={project.slug} onClick={() => setDraft({ ...project })}><span className="status-dot status-published" aria-hidden="true" /><span className="author-article-main"><strong>{project.name}</strong><span>{project.status} · {project.category} · {project.stack.slice(0, 3).join(", ")}</span></span><span className="author-row-arrow">Edit <Edit3 size={14} aria-hidden="true" /></span></button>)}{!filtered.length && !busy && <div className="author-empty"><p>No projects found.</p><p>Try another search or create a new project.</p></div>}</div>}{draft && <ProjectEditor draft={draft} busy={busy} onChange={update} onCancel={() => setDraft(null)} onSave={() => void save()} />}</section>;
}

function ProjectEditor({ draft, busy, onChange, onCancel, onSave }: { draft: Project; busy: boolean; onChange: (field: keyof Project, value: string | string[]) => void; onCancel: () => void; onSave: () => void }) {
  return <div className="author-panel content-type-editor"><div className="author-editor-heading"><div><div className="eyebrow">Project editor</div><h2>{draft.name || "New project"}</h2></div><div className="author-editor-actions"><button type="button" className="author-quiet-button" onClick={onCancel}><X size={15} /> Close</button><button type="button" className="btn btn-primary" onClick={onSave} disabled={busy}><Save size={15} /> Save project</button></div></div><div className="author-form author-fields content-type-form"><label>Project name<input value={draft.name} onChange={(event) => onChange("name", event.target.value)} required /></label><label>Slug<input value={draft.slug} onChange={(event) => onChange("slug", event.target.value)} placeholder="project-slug" required /></label><div className="author-field-grid"><label>Category<input value={draft.category} onChange={(event) => onChange("category", event.target.value)} /></label><label>Status<input value={draft.status} onChange={(event) => onChange("status", event.target.value)} /></label></div><label>Short description<textarea rows={3} value={draft.description} onChange={(event) => onChange("description", event.target.value)} required /></label><label>Cover image URL<input value={draft.coverImage} onChange={(event) => onChange("coverImage", event.target.value)} /></label><div className="author-field-grid"><label>GitHub URL<input type="url" value={draft.github || ""} onChange={(event) => onChange("github", event.target.value)} /></label><label>Live demo URL<input type="url" value={draft.demo || ""} onChange={(event) => onChange("demo", event.target.value)} /></label></div><label>Technology stack<textarea rows={3} value={joinLines(draft.stack)} onChange={(event) => onChange("stack", splitLines(event.target.value))} /><small>One technology per line.</small></label><label>Problem<textarea rows={3} value={draft.problem} onChange={(event) => onChange("problem", event.target.value)} /></label><label>Solution<textarea rows={3} value={draft.solution} onChange={(event) => onChange("solution", event.target.value)} /></label><label>Architecture<textarea rows={4} value={joinLines(draft.architecture)} onChange={(event) => onChange("architecture", splitLines(event.target.value))} /><small>One architecture note per line.</small></label><label>Features<textarea rows={4} value={joinLines(draft.features)} onChange={(event) => onChange("features", splitLines(event.target.value))} /><small>One feature per line.</small></label><label>Lessons<textarea rows={3} value={draft.lessons} onChange={(event) => onChange("lessons", event.target.value)} /></label><div className="content-type-preview-links"><Link href={`/projects/${draft.slug}`} target="_blank" rel="noreferrer" className="author-quiet-button"><ExternalLink size={14} /> Preview project</Link><button type="button" className="author-danger-button" onClick={onCancel}><Trash2 size={14} /> Cancel editing</button></div></div></div>;
}

export function TopicManager() {
  const [topics, setTopics] = useState<TopicRecord[]>([]); const [sha, setSha] = useState(""); const [draft, setDraft] = useState<TopicRecord | null>(null); const [search, setSearch] = useState(""); const [busy, setBusy] = useState(true); const [error, setError] = useState(""); const [notice, setNotice] = useState("");
  async function load() { setBusy(true); setError(""); try { const response = await fetch("/api/author/topics", { cache: "no-store" }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Unable to load topics."); setTopics(Array.isArray(body.topics) ? body.topics : []); setSha(body.shas?.topics || ""); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to load topics."); } finally { setBusy(false); } }
  useEffect(() => { void load(); }, []);
  const filtered = useMemo(() => topics.filter((topic) => !search.trim() || `${topic.name} ${topic.slug} ${topic.shortName}`.toLowerCase().includes(search.trim().toLowerCase())), [topics, search]);
  function update(field: keyof TopicRecord, value: string) { setDraft((current) => current ? { ...current, [field]: value } : current); setError(""); setNotice(""); }
  async function save() { if (!draft) return; const errors = validateTopicInput(draft); if (errors.length) { setError(errors[0]); return; } setBusy(true); setError(""); try { const response = await fetch("/api/author/topics", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ topics: topics.some((item) => item.slug === draft.slug) ? topics.map((item) => item.slug === draft.slug ? draft : item) : [...topics, draft], sha }) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Unable to save topic."); setSha(body.sha || sha); setTopics((current) => current.some((item) => item.slug === draft.slug) ? current.map((item) => item.slug === draft.slug ? draft : item) : [...current, draft]); setDraft(null); setNotice("Topic saved to GitHub."); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to save topic."); } finally { setBusy(false); } }
  return <section className="author-content-manager container"><div className="author-masthead"><div className="eyebrow">Content management</div><h1>Topics</h1><p>Manage the existing category metadata used by public topic pages and article taxonomy.</p></div><div className="author-heading"><div><div className="eyebrow">Topic index</div><h2>Follow the threads.</h2><p>Articles currently relate to topics through their existing category frontmatter; project relationships remain schema-derived.</p></div><button className="btn btn-primary" type="button" onClick={() => setDraft({ ...blankTopic })}><Plus size={16} aria-hidden="true" /> New topic</button></div><Message error={error} notice={notice} /><div className="author-toolbar"><label className="author-search-field"><span className="sr-only">Search topics</span><Search size={15} aria-hidden="true" /><input aria-label="Search topics" placeholder="Search topics…" value={search} onChange={(event) => setSearch(event.target.value)} /></label></div>{busy && !topics.length ? <p>Loading topics…</p> : <div className="author-article-list">{filtered.map((topic) => <button type="button" className="author-article-row" key={topic.slug} onClick={() => setDraft({ ...topic })}><span className="status-dot status-published" aria-hidden="true" /><span className="author-article-main"><strong>{topic.name}</strong><span>{topic.shortName} · /topics/{topic.slug}</span></span><span className="author-row-arrow">Edit <Edit3 size={14} aria-hidden="true" /></span></button>)}{!filtered.length && !busy && <div className="author-empty"><p>No topics found.</p><p>Try another search or create a new topic.</p></div>}</div>}{draft && <div className="author-panel content-type-editor"><div className="author-editor-heading"><div><div className="eyebrow">Topic editor</div><h2>{draft.name || "New topic"}</h2></div><div className="author-editor-actions"><button type="button" className="author-quiet-button" onClick={() => setDraft(null)}><X size={15} /> Close</button><button type="button" className="btn btn-primary" onClick={() => void save()} disabled={busy}><Save size={15} /> Save topic</button></div></div><div className="author-form author-fields content-type-form"><label>Topic name<input value={draft.name} onChange={(event) => update("name", event.target.value)} required /></label><label>Slug<input value={draft.slug} onChange={(event) => update("slug", event.target.value)} disabled={topics.some((item) => item.slug === draft.slug)} required /><small>Existing topic slugs are identity keys for article categories and cannot be changed in place.</small></label><label>Short name<input value={draft.shortName} onChange={(event) => update("shortName", event.target.value)} required /></label><label>Description<textarea rows={4} value={draft.description} onChange={(event) => update("description", event.target.value)} required /></label><div className="content-type-preview-links"><Link href={`/topics/${draft.slug}`} target="_blank" rel="noreferrer" className="author-quiet-button"><ExternalLink size={14} /> Preview topic</Link><button type="button" className="author-danger-button" onClick={() => setDraft(null)}><Trash2 size={14} /> Cancel editing</button></div></div></div>}</section>;
}
